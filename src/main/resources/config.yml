# ============================================================
#  Crosschat — config.yml
#  This file is created automatically on first run.
#  Modify and restart (or reload) the proxy to apply changes.
# ============================================================

# Set to false to disable global chat entirely without removing the plugin.
enabled: true

# ---------------------------------------------------------------
# Chat format
# ---------------------------------------------------------------
# Placeholders:
#   {server}  — name of the backend server the sender is currently on
#   {player}  — sender's Minecraft username
#   {prefix}  — prefix from LuckPerms (empty string if not installed)
#   {suffix}  — suffix from LuckPerms (empty string if not installed)
#   {group}   — primary LuckPerms group (empty string if not installed)
#   {message} — the chat message
#               NOTE: message content is ALWAYS rendered as plain text,
#               so players cannot inject colour codes or formatting.
#
# Format styles (pick ONE, do not mix in the same format string):
#   Legacy &-codes  →  &c, &l, &r, &#ff0000  etc.
#   MiniMessage     →  <red>, <bold>, <#ff0000>, <gradient:red:blue>  etc.
#
# Default (legacy &-codes):
format: "&8[&b{server}&8] &7{prefix}{player}{suffix}&f: {message}"

# MiniMessage example — uncomment and remove the line above to use:
# format: "<dark_gray>[<aqua>{server}</aqua>]</dark_gray> <gray>{prefix}{player}</gray><white>: {message}</white>"

# ---------------------------------------------------------------
# Server display names & filter
# ---------------------------------------------------------------
# Format:  servername: "Display Text"
#   The key must match the server name in velocity.toml (case-insensitive).
#   The value is substituted for {server} in the format string, so you
#   can use colours and formatting here just like in the format above.
#
# Players on servers NOT listed here neither see nor send global messages.
# Leave the block with no entries to include ALL servers; {server} will
# then show the raw velocity server name uppercased.
#
# & code examples:
#   hub:      "&bHUB"
#   survival: "&a&lSURVIVAL"
#   farming:  "&eFARMING"
#
# MiniMessage examples (only if your format uses MiniMessage):
#   hub:      "<aqua>HUB</aqua>"
#   survival: "<green><bold>SURVIVAL</bold></green>"
#   farming:  "<yellow>FARMING</yellow>"
#
servers:
  hub: "&bHUB"
  survival: "&aSURVIVAL"
  farming: "&eFARMING"
